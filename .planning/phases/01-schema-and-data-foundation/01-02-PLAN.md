---
phase: 01-schema-and-data-foundation
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - prisma/schema.prisma
autonomous: false
requirements:
  - SUB-04

must_haves:
  truths:
    - "prisma migrate status shows the new migration as applied (not pending)"
    - "PlatformSubscription and PlatformCharge tables exist in the database"
    - "The @@unique([subscriptionId, month, year]) constraint exists on PlatformCharge"
    - "The Student model has a subscriptions relation field"
    - "Existing Student, Session, and Invoice row counts are unchanged after migration"
    - "prisma.platformSubscription and prisma.platformCharge are valid TypeScript identifiers (Prisma client regenerated)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "PlatformSubscription and PlatformCharge models, subscriptions relation on Student"
      contains: "model PlatformSubscription"
    - path: "prisma/migrations/"
      provides: "tracked migration SQL file for prisma migrate deploy on Supabase"
  key_links:
    - from: "prisma/schema.prisma PlatformSubscription"
      to: "prisma/schema.prisma Student"
      via: "studentId foreign key + subscriptions reverse relation"
      pattern: "studentId.*String.*Student.*relation"
    - from: "prisma/schema.prisma PlatformCharge"
      to: "prisma/schema.prisma PlatformSubscription"
      via: "subscriptionId foreign key + @@unique constraint"
      pattern: "@@unique.*subscriptionId.*month.*year"
---

<objective>
Extend prisma/schema.prisma with PlatformSubscription and PlatformCharge models, add the subscriptions reverse relation to Student, generate and apply the migration, and confirm the Prisma client is regenerated with the new types.

Purpose: All subsequent phases depend on these two tables. The migration must be tracked (via prisma migrate dev, not db push) so it can be replayed with prisma migrate deploy on Supabase.

Output: Updated prisma/schema.prisma, a migration directory under prisma/migrations/, and a regenerated Prisma client with PlatformSubscription and PlatformCharge types available.
</objective>

<execution_context>
@C:/Users/shams/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/shams/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-and-data-foundation/01-RESEARCH.md
@prisma/schema.prisma

<interfaces>
<!-- Existing schema — executor must NOT modify these models, only append -->
<!-- From prisma/schema.prisma -->

Existing models (read-only):
- Student: id (cuid), name (unique), subject, ratePerMin, currency, email?, active, sessions, invoices, createdAt
- Session: id, studentId (FK→Student), date, durationMin, amountCHF, calEventId?, month, year, notes?, createdAt
- Invoice: id, studentId (FK→Student), month, year, totalCHF, sessionIds, invoiceNumber, sentAt?, paidAt?, pdfPath?, createdAt — @@unique([studentId, month, year])
- TutorProfile: id ("default"), name, email, address, phone, iban, bankName, createdAt, updatedAt

Conventions observed:
- Primary keys: String @id @default(cuid())
- Timestamps: createdAt DateTime @default(now())
- Mutable records: updatedAt DateTime @updatedAt
- Month/year: Int (1-indexed months, calendar years)
- Compound unique: @@unique([fieldA, fieldB, fieldC]) — see Invoice
- Nullable fields: DateTime? Float? String? (append ?)
- No DB-level CHECK constraints in existing schema
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PlatformSubscription and PlatformCharge models to schema.prisma</name>
  <files>prisma/schema.prisma</files>
  <action>
    Append the following to prisma/schema.prisma — after the TutorProfile model block. Do NOT modify any existing model fields. The only change to an existing model is adding one virtual relation field to Student (no new column, no migration impact on Student table).

    **Step 1 — Add relation field to Student model:**
    Inside the existing `model Student { ... }` block, add this line after the `invoices Invoice[]` line:
    ```
    subscriptions PlatformSubscription[]
    ```

    **Step 2 — Append two new models after TutorProfile:**

    ```prisma
    model PlatformSubscription {
      id             String           @id @default(cuid())
      studentId      String
      student        Student          @relation(fields: [studentId], references: [id])
      amountCHF      Float
      billingMethod  String           // "invoice" | "direct"
      durationMonths Int              // 1 or 6
      startMonth     Int              // 1–12
      startYear      Int
      active         Boolean          @default(true)
      charges        PlatformCharge[]
      createdAt      DateTime         @default(now())
      updatedAt      DateTime         @updatedAt
    }

    model PlatformCharge {
      id             String               @id @default(cuid())
      subscriptionId String
      subscription   PlatformSubscription @relation(fields: [subscriptionId], references: [id])
      month          Int
      year           Int
      amountCHF      Float
      paidAt         DateTime?
      createdAt      DateTime             @default(now())

      @@unique([subscriptionId, month, year])
    }
    ```

    Key decisions:
    - billingMethod is String (not enum) to match existing schema convention (currency is also String)
    - No CHECK constraint on durationMonths — enforced by Phase 2 UI validation
    - paidAt is nullable (null = unpaid, set in Phase 4)
    - PlatformCharge has no updatedAt (charges are immutable once created; only paidAt changes — Phase 4 adds that update path)
    - Compound unique name will be `subscriptionId_month_year` (Prisma auto-names it this way) — referenced in lib/platform-charges.ts as `{ subscriptionId_month_year: { ... } }`
  </action>
  <verify>
    <automated>npx prisma validate 2>&1</automated>
  </verify>
  <done>
    `npx prisma validate` exits 0 with no errors. The schema file contains both model PlatformSubscription and model PlatformCharge blocks, and the Student model contains `subscriptions PlatformSubscription[]`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate and apply migration, regenerate Prisma client</name>
  <files>prisma/schema.prisma</files>
  <action>
    **Before running the migration, verify DIRECT_URL is set:**
    ```bash
    grep DIRECT_URL .env.local | head -1
    ```
    If DIRECT_URL is missing or blank, STOP and surface a checkpoint — do not attempt to run the migration without it. The STATE.md concern is: "Supabase migration must use DIRECT_URL (not pooled connection)".

    If DIRECT_URL is confirmed present, run:
    ```bash
    npx prisma migrate dev --name add_platform_subscription_and_charge
    ```

    This command:
    1. Detects the two new models and the relation field on Student
    2. Generates SQL in prisma/migrations/YYYYMMDDHHMMSS_add_platform_subscription_and_charge/migration.sql
    3. Applies the migration to the database
    4. Regenerates the Prisma client (equivalent to prisma generate)

    After migration succeeds, verify the client has the new types:
    ```bash
    npx prisma validate
    npx prisma migrate status
    ```

    Do NOT use `prisma db push` — it produces no migration file and cannot be replayed on Supabase.
    Do NOT use `prisma migrate deploy` for local dev — `migrate dev` is correct here.
  </action>
  <verify>
    <automated>npx prisma migrate status 2>&1</automated>
  </verify>
  <done>
    `npx prisma migrate status` shows the migration `add_platform_subscription_and_charge` as applied (not pending). A migration SQL file exists under prisma/migrations/. TypeScript compilation of any file that imports `@prisma/client` and uses `prisma.platformSubscription` or `prisma.platformCharge` does not produce type errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify migration applied and database is correct</name>
  <action>Human verification of migration results — run the steps in how-to-verify below and confirm the database state matches expectations before proceeding to Plan 03.</action>
  <what-built>
    Two new Prisma models (PlatformSubscription, PlatformCharge) added to schema.prisma. Migration generated and applied to the local/Supabase database. Prisma client regenerated with new types.
  </what-built>
  <how-to-verify>
    1. Run `npx prisma migrate status` — confirm the new migration shows as "Applied"
    2. Open Supabase Studio (or run `npx prisma studio`) and verify `PlatformSubscription` and `PlatformCharge` tables exist with the correct columns
    3. Verify existing table row counts are unchanged: check Student, Session, Invoice tables still have their data
    4. Check that no TypeScript errors appear: `npx tsc --noEmit 2>&1 | head -20`
  </how-to-verify>
  <resume-signal>Type "approved" if migration applied cleanly, or describe any issues seen</resume-signal>
</task>

</tasks>

<verification>
1. `npx prisma validate` exits 0
2. `npx prisma migrate status` shows migration as Applied
3. prisma/migrations/ contains the new migration SQL file
4. `npx tsc --noEmit` has no errors related to PlatformSubscription or PlatformCharge (new types are available)
5. Student, Session, Invoice data intact (verified manually via Supabase Studio or prisma studio)
</verification>

<success_criteria>
- PlatformSubscription table exists in database with columns: id, studentId, amountCHF, billingMethod, durationMonths, startMonth, startYear, active, createdAt, updatedAt
- PlatformCharge table exists with columns: id, subscriptionId, month, year, amountCHF, paidAt, createdAt and @@unique([subscriptionId, month, year]) constraint
- Student table has a PlatformSubscription foreign key relation (no new column on Student itself)
- Migration is tracked in prisma/migrations/ (not just db push)
- Prisma client types include platformSubscription and platformCharge accessors
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-and-data-foundation/01-02-SUMMARY.md`
</output>
